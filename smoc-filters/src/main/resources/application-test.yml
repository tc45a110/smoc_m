spring:
  cloud:
    nacos:
      discovery:
        server-addr: 172.28.235.186:8848
    gateway:
      #Global http timeouts.must be specified in milliseconds. must be specified as a java.time.Duration
      httpclient:
        connect-timeout: 15000
        response-timeout: 5s

  #**********************************************************
  # Redis
  #**********************************************************
  redis: #redis配置
    host: 172.28.235.188
    port: 6555
    password: Dj!bm%Mk8DyP
    database: 1
    timeout: 3000
    jedis:
      pool:
        max-active: 8
        max-wait: 2000
        max-idle: 8
        min-idle: 0
        time-between-eviction-runs: 1000

logging:
  config: classpath:logback-spring.xml
  path: /alidata/smoc/smoc-log/smoc-filters-logs
  level:
    root: info